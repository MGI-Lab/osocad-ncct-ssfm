# Main-branch manuscript synchronization — 20 September 2026

This post-release update starts from public commit
`984c94df7cbc0f04b27a409ebe3c502ffb8b7fe8` (`v1.0.1`). It updates `main`
without moving the existing `v1.0.0` or `v1.0.1` tags and without creating a
new Release.

## Synchronized content

- All 25 retained comparison records preserve their unrounded P values. The 24
  Table 2 comparison cells now use the manuscript convention: values of 0.001
  or greater are rounded to three decimal places; smaller values are shown as
  `P < 0.001` with the calculated three-significant-digit scientific-notation
  value in parentheses.
- Figure 3b,d,f uses the approved compact labels (`P < 0.001`, `P = 0.004` and
  `P = 0.091`). The approved PNG, PDF and SVG files were synchronized together.
- Extended Data Figure 2d retains `Real world (n=2388)` and now uses
  `Participants` and `Predicted probability` without the removed x-axis
  qualifier. Its PNG, PDF and SVG files were synchronized together.
- The documented model environment now records Python v.3.10.19 and PyTorch
  v.2.7.0, with the remaining model packages referenced to manuscript Extended
  Data Table 3.
- The documented DICOM workflow now distinguishes pydicom file reading from
  SimpleITK/GDCM series discovery, `ImageSeriesReader` assembly and compressed
  NIfTI output. No unsupported library version or de-identification claim was
  added.

## Verification

- `python code/validation/verify_submission.py`: PASS, 567 checks and 58
  manifest-controlled artifacts.
- `python code/plotting/build_github_english_release.py --check`: PASS.
- `python -m unittest discover -s tests -v`: PASS, 42 tests, using the package
  versions pinned in `requirements-validation.txt`.
- The final four panels were matched to their independently reviewed local
  deliverables by SHA-256 before synchronization.

These checks do not rerun model inference or clinical statistical analyses.
No patient-level study data, DICOM/NIfTI data, workbooks or case-level
predictions were added.
